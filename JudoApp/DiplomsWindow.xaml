<Window x:Class="JudoApp.DiplomsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:JudoApp"
        mc:Ignorable="d"
        Title="Дипломы" MinHeight="800" MinWidth="500">
    <Window.Resources>
        <DataTemplate x:Key="PageTemplate">
            <Grid Margin="40">
                <Canvas Height="670" Width="450">
                    <Path Fill="#FF8B00" Stretch="Fill" HorizontalAlignment="Center" Height="490" VerticalAlignment="Top" Canvas.Left="231.933" Width="218">
                        <Path.Data>
                            <PathGeometry>
                                <PathFigure StartPoint="300,0" IsClosed="True">
                                    <LineSegment Point="450,0"/>
                                    <LineSegment Point="450,700"/>
                                    <LineSegment Point="420,660"/>
                                </PathFigure>
                            </PathGeometry>
                        </Path.Data>
                    </Path>

                    <Path Fill="#FCE300" Stretch="Fill" HorizontalAlignment="Center" VerticalAlignment="Center" Canvas.Top="434" Canvas.Left="121" Height="236" Width="329">
                        <Path.Data>
                            <PathGeometry>
                                <PathFigure StartPoint="400,640" IsClosed="True">
                                    <LineSegment Point="450,650"/>
                                    <LineSegment Point="450,750"/>
                                    <LineSegment Point="150,750"/>
                                </PathFigure>
                            </PathGeometry>
                        </Path.Data>
                    </Path>
                </Canvas>
                <Border BorderBrush="Black" BorderThickness="3" Padding="10">
                    <Border BorderBrush="DarkGray" BorderThickness="1" Padding="20">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Text="Certificate" 
                               FontSize="36" FontWeight="Bold" 
                               HorizontalAlignment="Center" 
                               Foreground="Black"
                               Margin="0,20,0,30"/>

                            <StackPanel Grid.Row="1" HorizontalAlignment="Center">
                                <TextBlock Text="Competition" 
                                   FontSize="20"
                                   HorizontalAlignment="Center"/>
                                <TextBlock Text="Organizer" 
                                   FontSize="20"
                                   HorizontalAlignment="Center" 
                                   Margin="0,5,0,20"/>
                            </StackPanel>

                            <StackPanel Grid.Row="2" HorizontalAlignment="Center" 
                                VerticalAlignment="Center">
                                <TextBlock Text="JUDO" 
                                   FontSize="42" FontWeight="Bold" 
                                   HorizontalAlignment="Center"
                                   Margin="0,0,0,40"
                                   Foreground="Black"/>
                                <StackPanel HorizontalAlignment="Center" Margin="0,20,0,0">
                                    <TextBlock 
                                       FontSize="16" FontWeight="Medium"
                                       HorizontalAlignment="Center">
                                        <Run Text="Gender: "/>
                                        <Run Text="{Binding Gender}"/>
                                    </TextBlock>
                                    <TextBlock 
                                       FontSize="16" FontWeight="Medium"
                                       HorizontalAlignment="Center" 
                                       Margin="0,3,0,0">
                                        <Run Text="Weight-Class: "/>
                                        <Run Text="{Binding Weight}"/>
                                    </TextBlock>
                                </StackPanel>
                            </StackPanel>

                            <StackPanel Grid.Row="3" HorizontalAlignment="Center">
                                <TextBlock Text="1st Place" 
                                   FontSize="28" FontWeight="Bold"
                                   HorizontalAlignment="Center"
                                   Margin="0,0,0,15"/>
                                <TextBlock Text="{Binding FIO}" 
                                   FontSize="22" FontWeight="SemiBold"
                                   HorizontalAlignment="Center"
                                   Margin="0,0,0,10"/>
                                <TextBlock Text="{Binding Sportsclubs}" 
                                   FontSize="16"
                                   HorizontalAlignment="Center"/>
                            </StackPanel>

                            <Viewbox Grid.Row="4" Width="130" Stretch="Uniform" HorizontalAlignment="Left">
                                <Image Source="/logo.jpg"/>
                            </Viewbox>
                        </Grid>
                    </Border>
                </Border>
            </Grid>
        </DataTemplate>
    </Window.Resources>
    <Grid>
        <DocumentViewer x:Name="documentViewer">
        </DocumentViewer>
    </Grid>
</Window>
